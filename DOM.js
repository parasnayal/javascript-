console.log(" This tutorial is all about the DOM in javascript ");
// DOCUMENT => is an object that provides access to all html elements of a document.When an HTML document is loaded into a browser window, then it becomes a document object.The document object stores the elements of an HTML document, such as HTML, HEAD, BODY, and other HTML tags as objects.


// DOM=> DOCUMENT OBJECT MODEL 
// It is an interface that allows a programming language to manipulate the content , structure and style of website.
// The DOM is often referred to as the DOM tree, and consists of a tree of objects called nodes

{/* <a href="index.html">Home</a>  */}
// a is the tag
// href is the attribute
// index.html is the attribute value
// Home is the text.
// Everything between the opening and closing tag combined make the entire HTML element.


// NODES=> All items in the DOM are defined as nodes
// three main nodes
// (1) => Element nodes
// (2) => Text nodes
// (3) => Comment nodes

// The html element node is the parent node. head and body are siblings, children of html
// body contains three child nodes, which are all siblings 
// he document itself is a document node, which is the root of all other nodes
